{
  "name": "@sandstorm/telemetry",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun ../../scripts/build.ts --format both",
    "dev": "bun --watch ../../scripts/build.ts --format both",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sandstorm/core": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "bun-types": "^1.0.0"
  }
}
